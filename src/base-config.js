/**
 * @type {import('prettier').Config}
 */
const baseConfig = {
  singleQuote: true,
  useTabs: false,
  tabWidth: 2,
  trailingComma: 'none',
  arrowParens: 'always',
  printWidth: 80,
  endOfLine: 'lf'
};

export default baseConfig;
